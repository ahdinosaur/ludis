services:
  redis:
    image: docker.io/redis:7
    restart: unless-stopped
    volumes:
      - redis-data:/data

  web:
    image: docker.io/nginx:1.27
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - redis

volumes:
  redis-data:
